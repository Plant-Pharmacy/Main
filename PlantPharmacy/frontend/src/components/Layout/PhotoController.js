
// Handles logic related to receiving user uploaded image, sending it to server, and displaying on frontend
// Still need to decide: 
// should this class behave as a parent to ImageView and UploadView or as a Utility class?
class PhotoController {

    // returns image
    viewImage = () => {

    }

    // takes photo file and serializes (prepares for sending over HTTP)
    serialize = (photoFile) => {

    }

    // sends serialized photo over HTTP and returns response from server
    postPhoto = () => {

    }
}

export default PhotoController;